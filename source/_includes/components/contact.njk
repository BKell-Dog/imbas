{% set contactLabel = contactLabel | default("What are you building?") %}
<div class="contact-form" id="contact-form" data-w3f-key="{{ site.web3formsKey }}">
  <link rel="stylesheet" href="/css/contact.css">
  <label for="project">{{ contactLabel }}</label>
  <textarea id="project-description" name="message" rows="3"></textarea>

  <div class="email-row flex-row gap-10">
    <div class="email-field flex-row">
      X &nbsp; <input type="email" id="email" name="email" class="email-input"
                placeholder="Your email" autocomplete="email">
    </div>
    <button class="submit-btn red-button" id="submit-btn">Send</button>
  </div>

  <p id="form-status" aria-live="polite"></p>

  <div class="or">or</div>

  <div class="email-fallback flex-column">
    <span class="align-self-left">Write to our point of contact directly,</span>
    <a href="mailto:{{ site.email }}" class="align-self-center">will@imbashardtech.com</a>
    <span class="align-self-right">We read and respond to every email.</span>
  </div>
</div>

<script src="/scripts/submitEmail.js"></script>